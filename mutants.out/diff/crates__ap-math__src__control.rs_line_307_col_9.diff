--- crates/ap-math/src/control.rs
+++ delete - in shape_angle_vel_accel
@@ -299,17 +299,17 @@
         Postype::from(angle_desired_wrapped),
         angle_vel_desired,
         angle_accel_desired,
         Postype::from(angle),
         angle_vel,
         angle_accel,
         angle_vel_min,
         angle_vel_max,
-        -angle_accel_max,
+         /* ~ changed by cargo-mutants ~ */angle_accel_max,
         angle_accel_max,
         angle_jerk_max,
         dt,
         limit_total,
     )
 }
 
 /// Proportional near the target, square-root far from it. Upstream
