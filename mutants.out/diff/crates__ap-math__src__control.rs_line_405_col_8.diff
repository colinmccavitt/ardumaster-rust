--- crates/ap-math/src/control.rs
+++ delete ! in sqrt_controller_accel
@@ -397,17 +397,17 @@
 #[must_use]
 pub fn sqrt_controller_accel(
     error: f32,
     rate_cmd: f32,
     rate_state: f32,
     p: f32,
     second_ord_lim: f32,
 ) -> f32 {
-    if !is_positive(rate_cmd * rate_state) {
+    if  /* ~ changed by cargo-mutants ~ */is_positive(rate_cmd * rate_state) {
         return 0.0;
     }
     if !is_positive(second_ord_lim) {
         return -p * rate_state;
     }
     if !is_positive(p) {
         if is_zero(rate_cmd) {
             return 0.0;
