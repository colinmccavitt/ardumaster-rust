--- crates/ap-math/src/control.rs
+++ replace / with * in sqrt_controller_accel
@@ -410,17 +410,17 @@
     }
     if !is_positive(p) {
         if is_zero(rate_cmd) {
             return 0.0;
         }
         return -(second_ord_lim / rate_cmd.abs()) * rate_state;
     }
 
-    let linear_dist = second_ord_lim / sq(p);
+    let linear_dist = second_ord_lim * /* ~ changed by cargo-mutants ~ */ sq(p);
     if error.abs() <= linear_dist {
         return -p * rate_state;
     }
     if is_zero(rate_cmd) {
         return 0.0;
     }
     -(second_ord_lim / rate_cmd.abs()) * rate_state
 }
