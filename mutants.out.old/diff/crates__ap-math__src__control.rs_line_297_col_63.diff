--- crates/ap-math/src/control.rs
+++ replace - with + in shape_angle_vel_accel
@@ -289,17 +289,17 @@
     angle_accel: &mut f32,
     angle_vel_min: f32,
     angle_vel_max: f32,
     angle_accel_max: f32,
     angle_jerk_max: f32,
     dt: f32,
     limit_total: bool,
 ) -> Result<(), ShapeError> {
-    let angle_desired_wrapped = angle + wrap_pi(angle_desired - angle);
+    let angle_desired_wrapped = angle + wrap_pi(angle_desired + /* ~ changed by cargo-mutants ~ */ angle);
     shape_pos_vel_accel(
         Postype::from(angle_desired_wrapped),
         angle_vel_desired,
         angle_accel_desired,
         Postype::from(angle),
         angle_vel,
         angle_accel,
         angle_vel_min,
